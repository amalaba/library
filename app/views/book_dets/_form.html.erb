<%= form_for(@book_det) do |f| %>
  <% if @book_det.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_det.errors.count, "error") %> prohibited this book_det from being saved:</h2>

      <ul>
      <% @book_det.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :author %><br />
    <%= f.text_field :author %>
  </div>
  <div class="field">
    <%= f.label :available %><br />
    <%= f.check_box :available %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
