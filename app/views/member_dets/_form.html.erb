<%= form_for(@member_det) do |f| %>
  <% if @member_det.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member_det.errors.count, "error") %> prohibited this member_det from being saved:</h2>

      <ul>
      <% @member_det.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :age %><br />
    <%= f.number_field :age %>
  </div>
  <div class="field">
    <%= f.label :sex %><br />
    <%= f.text_field :sex %>
  </div>
  <div class="field">
    <%= f.label :Address %><br />
    <%= f.text_area :Address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
